# macOS system files
.DS_Store
._*
.Spotlight-V100
.Trashes

# IDE configuration (PyCharm)
.idea/

# Python bytecode and cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment (if you use one, e.g., venv, .venv, env)
venv/
.venv/
env/
*.env

# Distribution / packaging
.eggs/
*.egg-info/
dist/
build/
*.egg

# Downloaded or generated files (Optional - decide if you need them)
# If you don't want the contents of 'downloads' tracked:
downloads/*
# If you don't want the contents of 'uploads' tracked:
uploads/*
# If you want to keep the directories but ignore contents:
# downloads/*
# !downloads/.gitkeep
# uploads/*
# !uploads/.gitkeep
# (Create empty .gitkeep files in these dirs if needed)